# 投資評價輔助系統 — 規劃書

根據孫慶龍三合一班課程（四大報表、財報卸妝、財報領先指標、六種本益比、股價淨值比、PEG、企業評價模型、實戰班）整理出的邏輯，設計成一套疊加在現有 `my-finance-dashboard` 之上的投資評價輔助模組。

---

## 一、系統定位

**這是什麼**：一套「估值試算 + 財報體檢 + 選股篩選 + 進出場提示 + 組合管理」的個人決策輔助工具。所有輸出都是「計算結果」與「是否符合規則」，不是「買/賣建議」。

**這不是什麼**：
- 不是自動選股/自動下單系統，最終判斷永遠由你自己做
- 不是即時看盤軟體（沿用現有「股票分頁不看盤」的定位）
- DCF（內在價值法）這類對假設高度敏感的工具，介面上要清楚標示「情境試算，非單一答案」

**使用者**：僅你自己，不做多租戶考量。

**與現有系統的關係**：現有股票分頁是「持股記錄 + 損益追蹤」（[app.js](app.js) 中 `stockPositions`／`loadStocks`／Apps Script 現價），本系統疊加「評價」「篩選」「決策輔助」層，兩者共用持股資料，互不取代。

---

## 二、應用情境（Use Cases）

| 情境 | 描述 | 對應模組 |
|---|---|---|
| A. 持股健檢 | 每週打開系統，看目前持股的估值位階（特價～瘋狂六級）有沒有變化 | 模組2 估值引擎 |
| B. 找新標的 | 定期看「選股策略卡」跑出的候選名單（合約負債選股法／PEG改良式／冷門股／存股+KD黃金交叉） | 模組4 選股策略卡 |
| C. 個股體檢 | 看到一檔感興趣的股票，快速檢查財報有沒有紅旗（存貨異常、業外收益佔比過高、無形資產風險等） | 模組3 財報紅旗檢查 |
| D. 買進時機判斷 | 決定要不要買之前，看是否符合技術面買點（周線站上20周均線）或危機入市條件 | 模組5 進出場訊號 |
| E. 停損/換股判斷 | 持股虧損時，用「賣出三大理由」檢查清單輔助判斷是否該停損或換股 | 模組5 進出場訊號 |
| F. 組合體檢 | 檢視整體持股是否過度集中、股票分類（成長/景氣循環/轉機/資產股）是否失衡、股債配置要不要調整 | 模組6 組合管理 |
| G.（選配）總經氛圍參考 | 看目前景氣燈號、M1B/M2、VIX 等宏觀指標，作大盤位階的參考 | 模組7 總經面板 |

---

## 三、模組清單

### 模組 1｜市場資料管線（基礎建設，其他模組都依賴它）
- **目的**：把全市場（上市+上櫃）的財務資料落地到 Supabase，供後續計算使用
- **輸入**：TWSE/TPEx OpenAPI（每日全市場 PE/PB/殖利率快照）＋ FinMind（歷史回補、財報三表細項）
- **輸出**：`market_daily_valuation`、`financial_statements_quarterly` 等原始資料表
- **排程**：每日快照用 GitHub Actions cron（比照現有 `backup-export.mjs` 的每日備份模式）；財報細項資料季更即可
- **優先度**：★★★★★ 必須最先做，其他模組都建立在這上面

### 模組 2｜估值引擎
- **目的**：把六種本益比法、六種淨值比法、PEG、存股口訣公式化，算出「特價／便宜／合理下緣／合理上緣／昂貴／瘋狂」六級價格帶
- **子功能**：
  - 固定式／滾動式／預估式 × 本益比／淨值比
  - PEG（Peter Lynch／Jim Slater 兩種門檻）
  - 存股口訣（357／456／4567，依股票類型自動套用對應版本）
  - DCF內在價值法（選配，需明確標示「情境試算」）
  - 葛林布雷神奇公式排序（選配）
- **輸出**：`valuation_bands_cache`（每檔股票、每次計算的六級價格帶快照）
- **優先度**：★★★★☆ 第一個要做的計算模組，先套用在你目前持股上驗證公式正確性

### 模組 3｜財報紅旗檢查
- **目的**：自動化課程裡的「財報卸妝四技巧」
- **檢查項**：無形資產占總資產比重、業外收益占稅後淨利比重、存貨/應收帳款成長率 vs 營收成長率背離、現金股利發放率與營運現金流背離
- **輸出**：`red_flag_checks`（每檔股票的紅旗數量與細項，非黑即白的燈號）
- **備註**：「會計主管異動」這項無法結構化抓取，先不做自動化，留作人工檢查提示
- **優先度**：★★★☆☆ 第三順位，依賴模組1的財報細項資料

### 模組 4｜選股策略卡
- **目的**：把課程裡明確的量化選股規則做成可執行的篩選器，跑出候選名單
- **策略卡清單**：
  1. 合約負債選股法：合約負債年增>100%、佔股本比>20%、近4季無虧損、本益比<20倍
  2. PEG改良式（台股版）：近3月營收年增>30%、本益比<15倍（或10倍）、排除虧損、排除KY股
  3. 冷門股投資法：長期負債/股本<10%、本益比<9倍、日均成交量<300張
  4. 存股+KD黃金交叉：月KD低檔黃金交叉 + 近5年平均股利殖利率>7%
  5. 葛林布雷神奇公式：盈餘報酬率＋資本報酬率雙因子排序
- **輸出**：`screener_results`（每次跑批的候選名單快照，附帶命中的條件明細）
- **優先度**：★★★★☆ 直接對應你「廣蒐全市場找便宜標的」的核心需求，資料到位後優先做

### 模組 5｜進出場訊號輔助
- **目的**：技術面時機提示 + 賣出檢查清單（都是「提示」而非「訊號」，避免變成自動交易系統的觀感）
- **子功能**：
  - 買進提示：周線收盤價站上20周均線
  - 賣出提示：日線收盤價跌破20周均線
  - 危機入市階段標記（大盤估值位階參考）
  - 賣出檢查清單（人工勾選）：競爭對手是否轉強、公司誠信是否有疑慮、是否有重大總體風險
- **優先度**：★★☆☆☆ 第四順位，技術面資料（K線、均線）需另外接股價歷史來源

### 模組 6｜投資組合管理
- **目的**：組合層級的體檢，而非單一個股
- **子功能**：持股集中度提示（4-8檔為建議分散區間）、六大股票分類標記（快速成長/穩定成長/緩慢成長/景氣循環/轉機/資產股，手動標記為主）、股債配置比例參考、**決策紀錄／回顧**
- **決策紀錄／回顧**：每次依據估值位階、紅旗檢查或選股策略卡做出買賣判斷時，記一筆「當時為什麼這樣判斷」；過一段時間可回頭比對「當初的判斷後來對不對」。這是整個系統從「計算工具」變成「陪跑員」的關鍵——重點不是算得多準，而是讓使用者的判斷力隨時間被追蹤、被檢視、持續進步。
- **優先度**：★★☆☆☆ 第四順位，可與模組5同期做；決策紀錄成本低（僅一張筆記表），但建議跟模組2/4同步上線，這樣一開始就能養成記錄習慣，而不是等組合模組做完才開始記

### 模組 7｜總經領先指標面板（選配）
- **目的**：M1B/M2、景氣對策信號、VIX、美元指數等宏觀指標小面板
- **資料源**：政府開放資料平台（央行、國發會、經濟部統計處）
- **優先度**：★☆☆☆☆ 最低優先，跟個股決策關聯度較弱，可最後做或乾脆只放外部連結

---

## 四、資料模型（概念層）

實際 SQL schema 等進入 Phase 0 開發時再細化，這裡先列出需要新增的核心實體：

- `market_daily_valuation`：每日全市場 PE / PB / 殖利率快照（來源：TWSE/TPEx OpenAPI）
- `financial_statements_quarterly`：資產負債表/損益表/現金流量表關鍵科目，按股票+季度（來源：FinMind）
- `valuation_bands_cache`：算好的六級價格帶結果，按股票+計算日期+方法（固定/滾動/預估）
- `red_flag_checks`：紅旗檢查結果快照，按股票+檢查日期
- `screener_results`：選股策略卡跑批結果，按策略+跑批日期，含命中股票清單與命中條件明細
- `watchlist`：觀察中但尚未持有的股票清單（區別於現有 `stock_transactions` 的實際持股）
- `stock_notes`：使用者對個股的手動研究筆記／標記（例如六大分類標籤、賣出理由勾選記錄）
- `decision_log`：決策紀錄，按股票+日期+動作（買進/賣出/續抱觀察），記錄當下依據的估值位階/紅旗結果/策略卡命中情況＋文字理由；未來可回填「事後結果」欄位做回顧比對

---

## 五、開發階段

| 階段 | 內容 | 對應模組 | 產出 |
|---|---|---|---|
| Phase 0 | 資料基礎建設：每日快照排程 + FinMind 歷史回補 | 模組1 | Supabase 新表 + GitHub Actions 排程腳本 |
| Phase 1 | 估值引擎，先套用在現有持股上驗證 | 模組2 | 持股清單新增「估值位階」欄位 |
| Phase 2 | 全市場選股策略卡 | 模組4 | 新頁面：策略卡候選名單 |
| Phase 3 | 財報紅旗檢查 | 模組3 | 個股體檢頁 |
| Phase 4 | 進出場提示 + 組合管理 | 模組5、6 | 提示區塊 + 組合體檢頁 |
| Phase 5（選配） | 總經指標面板 | 模組7 | 小面板或外部連結 |

每個階段結束都要能獨立驗證（例如 Phase 1 完成後，拿你手上實際持股跟 Goodinfo 手動查的本益比區間對一次，確認公式沒算錯，再往下一階段推進）。

---

## 六、實際使用情境 Walkthrough

以「一週使用流程」為例，串起各模組：

1. **週一，持股健檢**：打開系統看持股清單，估值位階欄位顯示哪幾檔跑到「便宜」或「昂貴」，作為要不要加碼/減碼的第一層提示（模組2）。
2. **週中，看新候選**：打開「選股策略卡」頁面，看本週各策略跑出的候選名單，挑2-3檔有興趣的（模組4）。
3. **針對候選股做體檢**：打開個股體檢頁，依序看：財報紅旗有沒有異常（模組3）→估值位階是否真的便宜（模組2）→技術面是否接近買點（模組5）。
4. **決定要不要進場**：如果體檢過關，先加進 `watchlist` 觀察；等技術面提示出現（周線站上20周均線）再進場，進場後記錄到現有的持股交易分頁。
5. **持股中持續追蹤**：每季財報更新後，重新檢視存股口訣殖利率位階、估值位階有沒有變化。
6. **遇到虧損時**：打開賣出檢查清單，逐項勾選（競爭力/誠信/總體風險），輔助判斷停損或續抱，而不是憑感覺決定。
7. **每月/每季組合體檢**：看整體持股檔數與股票分類是否失衡，決定要不要調整股債配置比例。

---

## 七、Phase 0 實作（資料管線第一步）

已完成的部分：[scripts/market-snapshot-fetch.mjs](scripts/market-snapshot-fetch.mjs) + [.github/workflows/market-snapshot.yml](.github/workflows/market-snapshot.yml)，每個交易日 20:00（台灣時間）抓 TWSE + TPEx 全市場當日 PER/PBR/殖利率，寫入 `market_daily_valuation`。實際測過兩個端點都能正常回傳：

- TWSE：`https://openapi.twse.com.tw/v1/exchangeReport/BWIBBU_ALL`（欄位：`Date`民國年、`Code`、`Name`、`PEratio`、`DividendYield`、`PBratio`）
- TPEx：`https://www.tpex.org.tw/openapi/v1/tpex_mainboard_peratio_analysis`（欄位對應：`SecuritiesCompanyCode`、`CompanyName`、`PriceEarningRatio`、`YieldRatio`、`PriceBookRatio`）

### 你要做的事

**1. 在 Supabase SQL Editor 建表**（比照 `category_map` 的「公開唯讀、非個人資料」模式，這張表不是使用者資料，不需要 `user_id`）：

```sql
create table market_daily_valuation (
  id             bigint generated always as identity primary key,
  trade_date     date not null,
  stock_id       text not null,
  stock_name     text,
  market         text not null check (market in ('TWSE','TPEx')),
  pe_ratio       numeric(10,2),
  dividend_yield numeric(6,2),
  pb_ratio       numeric(10,2),
  created_at     timestamptz not null default now(),
  unique (trade_date, stock_id)
);

create index on market_daily_valuation (stock_id, trade_date);

alter table market_daily_valuation enable row level security;

-- 市場公開資料，登入使用者可以讀；寫入只靠 service role（GitHub Actions），不開放給前端
create policy "登入使用者可讀市場快照"
on market_daily_valuation for select
using (auth.role() = 'authenticated');
```

**2. 確認 GitHub Secrets 已有 `SUPABASE_SERVICE_ROLE_KEY`**（應該已經有了，`backup.yml` 也在用同一個）。

**3. 先手動跑一次驗證**：進 GitHub repo 的 Actions 分頁，手動觸發 `Market Valuation Snapshot`（`workflow_dispatch`），跑完後回 Supabase 確認 `market_daily_valuation` 有資料進來、筆數合理（上市+上櫃全部加起來約1700+筆）。

**4. 之後每個交易日會自動累積一筆快照**，大約一年後就能算出「近1年最高最低本益比/淨值比」，在等待自然累積的同時，Phase 0 的下一步是規劃 FinMind 歷史回補（一次性補齊近5年區間，不用等一年），這部分等這支腳本先穩定跑幾天確認沒問題後再談。

---

## 八、邊界與注意事項

- 所有輸出用「目前位階」「是否符合規則」呈現，不用「建議買進/賣出」這種字眼，避免產品定位滑向投資建議。
- DCF內在價值法對折現率假設極敏感（課程案例：台積電同一組數據，折現率5% vs 15%算出的企業價值差2.5倍），介面上要用明顯提示標註「僅供情境參考」。
- 「會計主管異動」「重大訊息公告」這類無法結構化的紅旗項目，不強行自動化，保留人工檢查提示欄位即可。
- FinMind 全市場資料回補是分批次的排程工作（受限於單股查詢+每小時額度），不能做成即時查詢，這點在 Phase 0 設計排程腳本時要納入。
